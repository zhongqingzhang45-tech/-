import { defineComponent as v, ref as m, computed as n, provide as C, renderSlot as h } from "vue";
import { useRoute as R, useRouter as g } from "vue-router";
import { injectSceneRouterStore as _ } from "./context.js";
const P = /* @__PURE__ */ v({
  __name: "provider",
  setup(y) {
    const o = R(), s = g(), r = m([]), i = n(() => r.value.filter((e) => e.routePath === o.path)), l = n(() => {
      const e = o.query.capture;
      return typeof e != "string" || e.length === 0 ? null : e;
    }), c = n(() => {
      const e = l.value;
      return e && i.value.some((t) => t.id === e) ? e : null;
    });
    function p(e) {
      const t = r.value.find((u) => u.id === e.id && u.routePath === e.routePath);
      if (t) {
        t.title !== e.title && (t.title = e.title);
        return;
      }
      r.value.push(e);
    }
    function d(e, t) {
      const u = r.value.findIndex((a) => a.id === t && a.routePath === e);
      u !== -1 && r.value.splice(u, 1);
    }
    async function f(e) {
      await s.replace({
        query: {
          ...o.query,
          capture: e
        }
      });
    }
    return C(_, {
      activeCaptureRootId: c,
      currentRouteCaptureRoots: i,
      navigateToCaptureRoot: f,
      registerCaptureRoot: p,
      unregisterCaptureRoot: d
    }), (e, t) => h(e.$slots, "default");
  }
});
export {
  P as default
};
