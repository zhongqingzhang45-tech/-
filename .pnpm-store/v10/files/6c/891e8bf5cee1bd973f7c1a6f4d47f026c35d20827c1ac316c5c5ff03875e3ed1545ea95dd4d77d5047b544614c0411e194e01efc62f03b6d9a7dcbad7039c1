import { defineComponent as f, ref as l, onMounted as m, onUnmounted as g, computed as o, openBlock as z, createElementBlock as S, normalizeClass as r, createElementVNode as d, normalizeStyle as u, renderSlot as y } from "vue";
import { computeScenarioCanvasScale as _ } from "../runtime/scene-canvas.js";
const H = /* @__PURE__ */ f({
  __name: "canvas",
  props: {
    width: { default: 1920 },
    height: { default: 1080 },
    scaleMultiplier: { default: 1 }
  },
  setup(v) {
    const e = v, t = l(null), s = l(0), c = l(0);
    let a = null;
    function i() {
      s.value = t.value?.clientWidth ?? window.innerWidth, c.value = t.value?.clientHeight ?? window.innerHeight;
    }
    m(() => {
      i(), window.addEventListener("resize", i), "ResizeObserver" in window && t.value && (a = new ResizeObserver(() => i()), a.observe(t.value));
    }), g(() => {
      window.removeEventListener("resize", i), a?.disconnect();
    });
    const n = o(() => _({
      viewportWidth: s.value,
      viewportHeight: c.value,
      canvasWidth: e.width,
      canvasHeight: e.height,
      scaleMultiplier: e.scaleMultiplier
    })), h = o(() => ({
      width: `${e.width * n.value}px`,
      height: `${e.height * n.value}px`,
      transform: "translate(-50%, -50%)"
    })), p = o(() => ({
      width: `${e.width}px`,
      height: `${e.height}px`,
      transform: `scale(${n.value})`
    }));
    return (w, $) => (z(), S("main", {
      ref_key: "canvasRoot",
      ref: t,
      class: r([
        "relative h-screen w-full overflow-hidden"
      ])
    }, [
      d("div", {
        class: r([
          "absolute left-1/2 top-1/2"
        ]),
        style: u(h.value)
      }, [
        d("div", {
          "data-scenario-canvas-surface": "",
          class: r([
            "origin-top-left"
          ]),
          style: u(p.value)
        }, [
          y(w.$slots, "default")
        ], 4)
      ], 4)
    ], 512));
  }
});
export {
  H as default
};
