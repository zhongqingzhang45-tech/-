import { ref as h, computed as n, unref as g } from "vue";
function G(a) {
  const r = h(""), t = n(() => g(a.scenes)), I = n(() => g(a.onNavigate)), l = n(() => r.value.trim().toLowerCase()), d = n(() => t.value.find((e) => e.id === a.currentSceneId.value)), m = n(() => d.value?.title ?? "Unknown scene"), u = n(() => t.value.findIndex((e) => e.id === a.currentSceneId.value)), c = n(() => u.value > 0 ? t.value[u.value - 1] : void 0), v = n(() => u.value >= 0 && u.value < t.value.length - 1 ? t.value[u.value + 1] : void 0), x = n(() => c.value !== void 0), N = n(() => v.value !== void 0), s = n(() => l.value ? t.value.filter((e) => e.id.toLowerCase().includes(l.value) || e.title.toLowerCase().includes(l.value)) : t.value), w = n(() => {
    const e = s.value, f = a.currentSceneId.value, S = e.find((o) => o.id === f);
    return S ? [
      S,
      ...e.filter((o) => o.id !== f)
    ] : e;
  });
  function i(e) {
    I.value(e);
  }
  function L() {
    c.value && i(c.value.id);
  }
  function C() {
    v.value && i(v.value.id);
  }
  return {
    activeScene: d,
    activeSceneLabel: m,
    canGoNext: N,
    canGoPrev: x,
    filteredScenes: s,
    goNext: C,
    goPrev: L,
    goToScene: i,
    nextScene: v,
    paletteItems: w,
    prevScene: c,
    searchQuery: r
  };
}
export {
  G as useSceneNavigation
};
