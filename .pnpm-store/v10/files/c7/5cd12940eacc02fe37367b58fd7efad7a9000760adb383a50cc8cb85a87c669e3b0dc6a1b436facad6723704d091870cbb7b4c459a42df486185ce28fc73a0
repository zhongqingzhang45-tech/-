import { CommonRequestOptions, WithUnknown } from '@xsai/shared';

interface GenerateSpeechOptions extends CommonRequestOptions {
    input: string;
    /** @default `mp3` */
    responseFormat?: 'aac' | 'flac' | 'mp3' | 'opus' | 'pcm' | 'wav';
    /** @default `1.0` */
    speed?: number;
    voice: string;
}
declare const generateSpeech: (options: WithUnknown<GenerateSpeechOptions>) => Promise<ArrayBuffer>;

export { generateSpeech };
export type { GenerateSpeechOptions };
