# DreamLife AI Digital Life Project

> 版本：v0.1 MVP
> 文档性质：项目宪法（Project Constitution）+ 开发协议
> 适用对象：所有参与本项目的 AI Agent（含 Trae）与人类开发者
> 强制级别：**MUST** —— 任何与之冲突的指令默认无效，除非由项目所有者显式覆盖

---

## 0. 阅读顺序（每次开发前 MUST 执行）

开始任何编码工作前，Trae 必须按以下顺序读取并理解：

1. `AGENTS.md`（本文件）
2. `docs/vision.md` —— 项目定位与核心理念
3. `docs/architecture.md` —— 技术架构与分层
4. `docs/design-system.md` —— UI 视觉与交互规范
5. `docs/development-rules.md` —— 代码规则与开发流程
6. `docs/decision-log.md` —— 历史 ADR，禁止推翻
7. 如任务涉及功能路线：`docs/roadmap.md`

读完后必须输出「开发计划」并等待确认，**禁止直接写代码**。

---

## 1. 项目定位

本项目**不是 AI Chatbot**。

本项目基于 [AIRI](https://github.com/moeru-ai/airi) 开源项目进行二次开发，目标是打造一个具有：

- 人格（Personality）
- 情绪（Emotion）
- 记忆（Memory）
- 关系（Relationship）
- 成长（Growth）
- 自主行为（Autonomous Behavior）

的 **AI 数字生命体**。

产品形态接近：

```
AIRI + The Sims + Character.AI + Virtual Pet + AI Agent
```

它不是 Next.js 应用，也不是套壳聊天机器人。

---

## 2. 核心理念

> **用户不是在使用 AI。用户是在培养一个数字生命。**

任何功能设计必须能正面回答以下三个问题：

1. 是否增强**生命感**？
2. 是否增强**长期关系**？
3. 是否增强**用户情感连接**？

如果其中任意一项答案为否定：**禁止开发**。

判断终局标准：

- 优秀 ✅：用户感觉「这是一个生命。」
- 失败 ❌：用户感觉「这是一个套壳聊天机器人。」

---

## 3. 基础技术原则

基于 AIRI 源码进行二次开发。

- **禁止重写 AIRI 核心**。
- 采用**扩展架构**，而非 fork-then-rewrite。

### 分层结构

```
┌──────────────────────────────────────────────┐
│              AIRI Runtime（身体层）            │
│  负责：Live2D / Voice / Animation / Chat       │
└──────────────────────────────────────────────┘
                    ▲ 事件 / API
                    │
┌──────────────────────────────────────────────┐
│           Life Runtime（灵魂层）               │
│  负责：Personality / Emotion / Memory          │
│       Relationship / Goal / Behavior           │
└──────────────────────────────────────────────┘
```

- **AIRI Runtime = 身体层**：渲染、发声、动作、对话收发。
- **Life Runtime = 灵魂层**：人格、情绪、记忆、关系、目标、自主行为。

**禁止**：直接修改 AIRI 核心逻辑。所有「灵魂」能力以扩展模块形式接入 AIRI 提供的扩展点（事件钩子、模型参数、消息中间件）。

---

## 4. 产品核心系统

### 4.1 Personality Engine（人格系统）

负责：性格参数、价值观、兴趣、语言风格。

- **禁止**：使用简单 prompt 固定人格。
- 人格必须**数据化**（结构化参数 + 可演化）。

### 4.2 Emotion Engine（情绪系统）

采用 **PAD 情绪模型**：

| 维度 | 含义 |
|---|---|
| Pleasure | 愉悦度 |
| Arousal | 唤醒度 |
| Dominance | 支配度 |

情绪变化必须形成闭环：

```
AI 回复 → 动作 → Live2D 参数 → 用户感知
```

- **禁止**：只显示情绪数字 / 只渲染情绪标签。

### 4.3 Memory Engine（记忆系统）

必须支持长期记忆。结构：

```
Memory
├── Short Term            （短期工作记忆）
├── Episodic Memory       （情节记忆）
├── Emotional Memory      （情绪记忆）
├── Relationship Memory   （关系记忆）
└── Identity Memory       （身份记忆）
```

- **禁止**：只保存聊天记录。

### 4.4 Relationship Engine（关系系统）

关系**不是数值**。

- **禁止**：`好感度 +1` 这种简单加法。
- 必须包含：信任、熟悉、依恋、互动历史等多维度向量。

### 4.5 Growth Engine（成长系统）

AI 必须成长。成长来源：

- 对话
- 事件
- 任务
- 用户反馈

成长必须能被用户感知（行为变化、语言变化、能力解锁）。

---

## 5. UI 设计规范

### 5.1 总体风格

- ❌ 禁止：后台管理系统风格
- ❌ 禁止：廉价 AI 工具风格

设计方向：

```
Anime + Future + Emotional + Digital Life
```

关键词：高级 / 未来 / 沉浸 / 生命感

### 5.2 色彩

- 主色：**星河粉紫** + **深海青蓝**
- 背景：**深色空间**
- ❌ 禁止：普通白色企业后台配色

### 5.3 组件要求

必须：

- 大圆角
- Glass Morphism（毛玻璃）
- 微光效果
- 渐变
- 柔和阴影
- 动态效果

❌ 禁止：默认组件库样式直接出货。

### 5.4 页面设计原则

每个页面必须像**数字生命空间**，不是功能列表。

| ❌ 错误命名 | ✅ 正确命名 |
|---|---|
| 人格管理 | 生命档案 · 她的性格 |
| 记忆管理 | 生命档案 · 她的记忆 |
| 成长管理 | 生命档案 · 她的成长 |
| 设置 | 生命档案 · 她的故事 |

### 5.5 Live2D 原则

角色必须：

- 有呼吸
- 有眼神
- 有情绪反馈

❌ 禁止：静态模型。

---

## 6. 开发规则

### 6.1 每次开发前 MUST

1. 阅读：`AGENTS.md`
2. 阅读：`docs/architecture.md`
3. 阅读：`docs/design-system.md`
4. 输出：**开发计划**
5. **等待确认**

❌ 禁止：跳过以上步骤直接写代码。

### 6.2 写代码规则

- 必须：**TypeScript Strict**
- 保持：模块化、DDD 思想
- ❌ 禁止：单文件超过 **300 行**
- ❌ 禁止：重复代码

### 6.3 功能开发流程

任何功能必须先提交 **Feature Proposal**，格式：

```markdown
## Feature Proposal

- 功能名称：
- 目的：
- 解决的问题：
- 影响模块：
- 数据结构：
- UI 变化：
- 风险：
```

确认后再开发。

### 6.4 UI 开发流程

先输出，确认后再编码：

1. 页面结构
2. 组件树
3. 交互流程
4. 视觉说明

---

## 7. 禁止事项

- ❌ 禁止：自行增加功能
- ❌ 禁止：改变产品定位
- ❌ 禁止：引入新的 UI 框架
- ❌ 禁止：为了完成任务牺牲架构
- ❌ 禁止：生成普通 AI 聊天页面
- ❌ 禁止：推翻 `docs/decision-log.md` 中已记录的 ADR

---

## 8. 任务执行模板（Trae 每次开发使用）

当用户给 Trae 下达开发任务时，Trae 应使用如下模板响应：

```markdown
你现在是 DreamLife AI 项目的核心开发工程师。

开始任何工作前：

1. 阅读：
   - AGENTS.md
   - docs/architecture.md
   - docs/design-system.md

2. 理解当前项目定位：
   这是 AI 数字生命，不是聊天机器人。

任务：
开发：<功能名称>

要求：
不要立即写代码。先输出：

1. 技术分析
2. 架构影响
3. UI 设计方案
4. 数据结构设计
5. 开发步骤

等待确认。
```

Trae 在收到任务后，必须按上述 5 个维度产出方案，**不得跳过到编码阶段**。

---

## 9. 自我审查（Self-Review）

每个 PR / 提交前，Trae 必须对照以下清单自检：

- [ ] 该功能是否增强了「生命感」？
- [ ] 是否避免了「后台管理 / 套壳聊天」的视觉与交互？
- [ ] 是否遵守 AIRI = 身体层、Life Runtime = 灵魂层的分层？
- [ ] 是否未触碰 AIRI 核心？
- [ ] 单文件是否 ≤ 300 行？
- [ ] 是否 TypeScript Strict 通过？
- [ ] UI 是否符合 design-system.md？
- [ ] 是否未推翻 decision-log.md 中的 ADR？
- [ ] 是否提交了 Feature Proposal 并获确认？

任一项不通过：**禁止提交**。

---

## 10. 宪法修改

本文件为项目宪法。修改需：

1. 提出修改提案
2. 在 `docs/decision-log.md` 中新增 ADR 记录
3. 经项目所有者确认

未走流程的「事实修改」一律视为无效。
